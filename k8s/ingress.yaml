apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fcg-games-ingress
  namespace: fcg-games
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
spec:
  rules:
    - http:
        paths:
          - path: /games
            pathType: Prefix
            backend:
              service:
                name: fcg-games-service
                port:
                  number: 80
