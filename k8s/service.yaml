apiVersion: v1
kind: Service
metadata:
  name: fcg-games-service
  namespace: fcg-games
spec:
  selector:
    app: fcg-games
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
